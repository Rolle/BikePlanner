<%= form_for(:session, :url => sessions_path) do |f| %>
  <%= f.label :mail %>
  <%= f.text_field :mail, :class=>"input-medium" %>

  <%= f.label "Passwort" %><br />
  <%= f.password_field :password, :class=>"input-medium"%>
  
  <%= f.submit "Anmelden", :class=>"btn btn-primary"%>
<% end %>