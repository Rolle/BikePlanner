<table class="table table-condensed table-striped">
  <thead>
    <tr>
      <th>Vom</th>
      <th>Titel</th>
      <th>Text</th>
      <th>Link</th>
      <th>User</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.created_at.strftime("%d.%m.%y") %>
        <td><%= post.title %></td>
        <td><%= post.text %></td>
        <td><%= post.link %></td>
        <td><%= post.user.name %></td>
        <td><%= link_to 'Anzeigen', post, :class => "btn btn-primary" %></td>
        <td><%= link_to 'Bearbeiten', edit_post_path(post), :class => "btn btn-primary" %></td>
        <td><%= link_to 'Löschen', post, :confirm => 'Gaaanz sicher?', :method => :delete, :class => "btn btn-primary" %></td>
      </tr>
    <% end %>
  </tbody>  
</table>